import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  container: {
    alignItems: 'center',
  },
  image: {
    width: '100%',
    alignSelf: 'center',
    resizeMode: 'contain',
    aspectRatio: 1.2
  },
  tags: {
    flexDirection: 'row',
  }
});

export default styles;
