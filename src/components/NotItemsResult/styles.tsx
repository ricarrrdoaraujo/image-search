import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  image: {
    height: '80%',
    paddingTop: 50,
    paddingBottom: 20,
    alignSelf: 'center',
  },
});

export default styles;
