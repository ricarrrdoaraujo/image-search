import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  image: {
    width: '80%',
    alignSelf: 'center',
    resizeMode: 'contain',
    aspectRatio: 1.2
  },
});

export default styles;
